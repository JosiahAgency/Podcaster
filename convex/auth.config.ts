const authConfig = {
    providers: [
        {
            domain: "https://informed-peacock-26.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;
